const state = {
    userType: 'general'
};

const getters = {
    userType: (state) => state.userType
};

const actions = {};

const mutations = {};

export default {
    state,
    getters, 
    actions, 
    mutations
};