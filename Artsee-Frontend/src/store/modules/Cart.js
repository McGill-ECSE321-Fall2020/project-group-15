const state = {
   customerCart : [],
};

const getters = {
    customerCart: (state) => state.customerCart,
};

const actions = {
    
};

const mutations = {
    
};

export default {
    state,
    getters, 
    actions, 
    mutations
};